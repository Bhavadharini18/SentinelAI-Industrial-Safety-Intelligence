# Datasets Directory

This folder contains CSV and JSON records used for modeling and training ML classification networks.

Initial rule-based calculations are deployed inside `intelligence-engine/risk_calculator.py`. Future iterations will train a Random Forest Classifier on datasets kept here to replace rule heuristics.
